{
  "name": "object-recognition-story-machine",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "export": "next build",
    "start": "next start",
    "lint": "eslint",
    "deploy": "npm run build && wrangler pages deploy out",
    "deploy:frontend": "npm run build && wrangler pages deploy out",
    "deploy:backend": "cd worker && wrangler deploy",
    "deploy:all": "./scripts/deploy.sh --all",
    "deploy:production": "./scripts/deploy.sh --all --production",
    "test:integration": "node mock-server.js & npm run dev",
    "test:api": "curl -X POST -F 'image=@test-images/test.png' http://localhost:3001"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "framer-motion": "^12.23.24",
    "multer": "^2.0.2",
    "next": "15.5.6",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.5.6",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
